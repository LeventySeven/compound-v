# STATE — <repo> — rewritten <YYYY-MM-DD HH:MM>

## Goal
<one sentence: what "done" looks like, as observable behavior. This is also the /goal condition.>

## Done
- <thing that works> — evidence: `<command>` exit 0, "<the proving output line>"

## Next
1. <the single next action, concrete enough to start without re-deriving anything: file, function, command>
2. <the one after that>

## Open decisions
- <what is genuinely blocked on a human — a credential, a deploy target, a product call — or "none">

## Verify with
```bash
<the exact command that proves the whole thing works end to end>
```

## Do not
- <approach already tried and rejected, and why. This is the part git history cannot recover.>
